# Vision Aid Backend - PRODUCTION Environment Configuration
# Change all values to your production settings

# Server Configuration
PORT=3000
NODE_ENV=production

# MongoDB Atlas Configuration (Production)
# Create a MongoDB Atlas cluster and use your connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vision-aid?retryWrites=true&w=majority

# YOLO Service Configuration
# Point to your production YOLO service
YOLO_SERVICE_URL=https://your-yolo-service.com
YOLO_SERVICE_TIMEOUT=30000

# OAuth Configuration (Update with production credentials)
# Get these from Google Cloud Console and GitHub Developer Settings
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

GITHUB_CLIENT_ID=your-production-github-client-id
GITHUB_CLIENT_SECRET=your-production-github-client-secret

# Callback URLs (Your production domain)
BACKEND_URL=https://api.yourdomain.com
FRONTEND_URL=https://yourdomain.com

# Security
# Generate a strong random string for production
JWT_SECRET=your-strong-production-jwt-secret-change-this

# Session Secret
SESSION_SECRET=your-strong-production-session-secret-change-this

# Optional: Email service (for password reset)
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASSWORD=your-email-password

# Optional: Logging service
LOG_LEVEL=info
